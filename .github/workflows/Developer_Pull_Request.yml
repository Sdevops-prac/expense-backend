name: Developer_Pull_Request
on:
  pull_request:
    branches:
      - 'main'
jobs:
  checks-bats-version:
    runs-on: self-hosted
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - run: ls
      - run: tail index.js
