name: Release_tags
on:
  push:
    tags:
      - 'v*'
jobs:
  checks-bats-version:
    runs-on: self-hosted
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - run: ls
      - run: tail index.js